import { TYPES } from "../types";

const initState = {
  list: [
    {
      id: 1,
      brandName: "ayla",
    },
    {
      id: 2,
      brandName: "sigra",
    },
  ],
};

export const productReducer = (state = initState, action) => {
  return state;
};
